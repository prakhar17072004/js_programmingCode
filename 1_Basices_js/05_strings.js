//console.log('prakhar'+' msihra');

// using string interpolation 
// string interpolation means embedding the variable or expression directly in string without using string concatenation (+).

let name = 'Prakhar mishra'

const repoCount = 40;
console.log(`hey ${name} is a self motivated and self dependent person `);

const userName = new String('Prakhar ')

console.log(userName.length);
console.log(userName.toLowerCase());
console.log(userName.toUpperCase());


